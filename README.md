# Prediction-of-Thermal-Conductivity-materials-via-ML
This ML pipeline predicts lattice thermal conductivity using AFLOW structural data. To enable rapid screening without costly DFT, it prevents physical data leakage by excluding dependent AEL/AGL outputs. It features a Ridge baseline, a tuned LightGBM ensemble, and SHAP analysis to reveal the true physical drivers of heat transport.
